{
  "name": "pet-shop",
  "version": "1.0.0",
  "description": "",
  "main": "truffle.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "dev": "lite-server",
    "deploy:local": "truffle deploy --network local --reset",
    "deploy:testnet": "truffle deploy --network testnet --reset",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@truffle/hdwallet-provider": "^1.2.0",
    "dotenv": "^8.2.0",
    "lite-server": "^2.3.0"
  },
  "dependencies": {
    "ethers": "^5.0.23"
  }
}
